/**
 * @format
 */

import { AppRegistry } from 'react-native';
import CoreRborn from './CoreRborn';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => CoreRborn);
